import { BindQueryParamsDirective } from './bind-query-params.directive';

describe('BindQueryParamsDirective', () => {
  it('should create an instance', () => {
    const directive = new BindQueryParamsDirective();
    expect(directive).toBeTruthy();
  });
});
